# Deployment specification
---
defaultSettings:
  useVersion: true

deployments:
  - artifact: monitor-web
    group: nl/zencode/monitor-app
    version: 0.1.0
    name: monitor-web
    target:
      test-cluster
